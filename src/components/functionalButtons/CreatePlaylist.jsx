export default function CreatePlaylistButton() {
  const createPlaylist = () => {
    // your create playlist logic here
  };

  return (
    <div className="card">
      <button onClick={createPlaylist}>Create Playlist</button>
    </div>
  );
}
